<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 370 100">
  <defs>
    <linearGradient id="blueAccent" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#7EC8F0"/>
      <stop offset="100%" stop-color="#2979D8"/>
    </linearGradient>
  </defs>

  <!-- T (first) -->
  <text x="0" y="88"
        font-family="Arial Black, Arial, sans-serif"
        font-size="95" font-weight="900"
        fill="#141850">T</text>

  <!-- Blue vertical accent between T and T -->
  <rect x="64" y="18" width="6" height="52" rx="3" fill="url(#blueAccent)"/>

  <!-- T (second) -->
  <text x="73" y="88"
        font-family="Arial Black, Arial, sans-serif"
        font-size="95" font-weight="900"
        fill="#141850">T</text>

  <!-- Person head above M -->
  <circle cx="213" cy="14" r="10" fill="#141850"/>

  <!-- M — body/legs of person figure -->
  <text x="163" y="90"
        font-family="Arial Black, Arial, sans-serif"
        font-size="95" font-weight="900"
        fill="#141850">M</text>

  <!-- PR in cyan -->
  <text x="262" y="84"
        font-family="Arial Black, Arial, sans-serif"
        font-size="70" font-weight="900"
        fill="#29C5E0">PR</text>
</svg>
